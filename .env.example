# Copy to .env and edit

FLASK_ENV=development
SECRET_KEY=change-me

# MariaDB connection string (create the DB/user in MariaDB, then point Flask at it)
# Format: mysql+pymysql://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=mysql+pymysql://miwalavie:miwalavie@127.0.0.1:3306/miwalavie

# Optional: set where uploaded product images are stored
UPLOAD_FOLDER=static/uploads

# WhatsApp no longer used; checkout uses in-app chat.
